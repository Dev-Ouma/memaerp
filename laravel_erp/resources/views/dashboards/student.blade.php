<div class="eyebrow">Student portal</div><h1 class="heading">Welcome, {{ auth()->user()->first_name }}.</h1><p class="sub">Your course progress, attendance and latest assessment results.</p>
<div class="grid4"><div class="stat"><div class="stat-head"><span>Admission number</span><i data-lucide="badge-check"></i></div><b class="compact-value">{{ $student?->admission_number ?? 'Pending' }}</b><small>Student identity</small></div><div class="stat"><div class="stat-head"><span>Course</span><i data-lucide="graduation-cap"></i></div><b class="compact-value">{{ $student?->course?->name ?? 'Unassigned' }}</b><small>Current programme</small></div><div class="stat"><div class="stat-head"><span>Attendance</span><i data-lucide="calendar-check"></i></div><b>{{ $attendanceRate }}%</b><small>Recorded sessions</small></div><div class="stat"><div class="stat-head"><span>Results</span><i data-lucide="chart-column"></i></div><b>{{ $results->count() }}</b><small>Published subjects</small></div></div>
<section class="panel" style="margin-top:18px"><div class="panel-head"><h2>My results</h2></div><div class="table-wrap"><table><thead><tr><th>Subject</th><th>Test</th><th>Exam</th><th>Total</th><th>Grade</th></tr></thead><tbody>@forelse($results as $result)@php($total=$result->total)<tr><td><strong>{{ $result->subject->name }}</strong></td><td>{{ $result->test_score }}</td><td>{{ $result->exam_score }}</td><td class="score {{ $total>=50?'good':'' }}">{{ $total }}%</td><td><span class="badge">{{ $total>=70?'A':($total>=60?'B':($total>=50?'C':($total>=40?'D':'E'))) }}</span></td></tr>@empty<tr><td colspan="5" class="empty">No results have been published yet.</td></tr>@endforelse</tbody></table></div></section>
