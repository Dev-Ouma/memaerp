@extends('layouts.app')
@section('title', $setup->name)
@section('section', 'Admissions · Admin Setups')
@section('content')
@php
    $active = $setup->versions->firstWhere('status', 'ACTIVE');
    $draft = $setup->versions->firstWhere('status', 'DRAFT');
    $latest = $setup->versions->first();
@endphp
<style>
.setup-breadcrumb{display:flex;align-items:center;gap:7px;color:var(--muted);font-size:12px;margin-bottom:13px}.setup-breadcrumb a{color:var(--secondary);text-decoration:none}.setup-breadcrumb svg{width:13px}.setup-titlebar{display:flex;justify-content:space-between;gap:20px;align-items:flex-start;margin-bottom:18px}.setup-titlebar h1{font-size:27px;margin:4px 0}.setup-titlebar p{margin:0;color:var(--muted);max-width:760px}.setup-key{display:inline-flex;margin-top:10px;font:11px ui-monospace,monospace;color:#52625a;background:#edf2ef;padding:5px 8px;border-radius:5px}.setup-state{display:flex;align-items:center;gap:7px;background:#fff;border:1px solid var(--line);border-radius:8px;padding:9px 12px;white-space:nowrap;font-weight:700}.setup-state svg{width:17px;color:var(--secondary)}.governance-strip{display:grid;grid-template-columns:repeat(4,1fr);background:#fff;border:1px solid var(--line);border-radius:10px;margin-bottom:18px}.governance-item{padding:15px 17px;border-right:1px solid var(--line)}.governance-item:last-child{border-right:0}.governance-item small,.governance-item strong{display:block}.governance-item small{color:var(--muted);font-size:10px;text-transform:uppercase;font-weight:800;letter-spacing:.05em}.governance-item strong{margin-top:5px;font-size:13px}.setup-detail-grid{display:grid;grid-template-columns:minmax(0,1.2fr) minmax(360px,.8fr);gap:18px}.section-heading{display:flex;gap:10px;align-items:flex-start}.section-heading span{width:34px;height:34px;border-radius:8px;background:#eaf3ef;color:var(--secondary);display:grid;place-items:center}.section-heading svg{width:17px}.section-heading h2{font-size:15px;margin:0}.section-heading p{font-size:11px;color:var(--muted);margin:2px 0 0}.config-form{padding:20px;display:grid;gap:17px}.form-label{display:flex;justify-content:space-between;gap:10px;font-size:12px;font-weight:800;margin-bottom:7px}.form-label small{font-weight:400;color:var(--muted)}.config-editor{width:100%;min-height:330px;border:1px solid #bccac2;border-radius:8px;background:#f8faf9;color:#17362a;padding:15px;font:12px/1.65 ui-monospace,SFMono-Regular,Menlo,monospace;tab-size:2;resize:vertical}.config-editor:focus{outline:3px solid #1e844928;border-color:var(--secondary);background:#fff}.reason-input{width:100%;border:1px solid #c8d2cc;border-radius:7px;padding:11px;font:inherit}.form-guidance{display:flex;gap:9px;padding:11px 12px;background:#fff8e8;border:1px solid #f0ddb6;border-radius:7px;color:#725319;font-size:12px}.form-guidance svg{width:17px;flex:none}.form-actions{display:flex;justify-content:space-between;align-items:center;gap:12px}.form-actions small{color:var(--muted);max-width:390px}.history-panel{overflow:hidden}.history-summary{padding:16px 18px;background:#f8faf9;border-bottom:1px solid var(--line);display:flex;justify-content:space-between}.history-summary small{color:var(--muted)}.version-card{padding:17px 18px;border-bottom:1px solid var(--line)}.version-card:last-child{border-bottom:0}.version-head{display:flex;justify-content:space-between;gap:10px}.version-number{display:flex;align-items:center;gap:8px}.version-number svg{width:16px}.version-number strong{font-size:14px}.status-chip{padding:4px 7px;border-radius:999px;font-size:10px;font-weight:800}.status-active{background:#e8f5ec;color:#1e713e}.status-draft{background:#fff3dc;color:#95600c}.status-inactive,.status-archived{background:#edf0ee;color:#59645e}.version-meta{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:12px 0;font-size:11px;color:var(--muted)}.version-meta span{display:flex;gap:5px}.version-meta svg{width:14px}.change-note{border-left:3px solid var(--accent);padding:7px 9px;background:#faf8f3;font-size:11px;margin:10px 0}.version-config{margin:10px 0}.version-config summary{cursor:pointer;color:var(--secondary);font-weight:700;font-size:11px}.version-config pre{white-space:pre-wrap;max-height:220px;overflow:auto;background:#102f38;color:#d7e9e1;padding:12px;border-radius:7px;font-size:10px}.publish-box{background:#f4f8f6;border:1px solid #d8e4dd;border-radius:8px;padding:12px;margin-top:12px}.publish-box h3{font-size:12px;margin:0 0 9px}.publish-fields{display:grid;grid-template-columns:1fr 1fr;gap:8px}.publish-fields label{font-size:10px;font-weight:800}.publish-fields input{width:100%;margin-top:4px;padding:8px;border:1px solid #c8d2cc;border-radius:6px}.publish-box .btn{margin-top:10px;width:100%;justify-content:center}.danger-action{display:flex;justify-content:space-between;align-items:center;gap:10px;margin-top:10px}.danger-action small{color:var(--muted)}.empty-version{text-align:center;padding:38px 20px}.empty-version span{width:48px;height:48px;margin:auto;border-radius:50%;display:grid;place-items:center;background:#edf2ef;color:var(--secondary)}.empty-version svg{width:22px}.empty-version h3{font-size:14px;margin:12px 0 4px}.empty-version p{color:var(--muted);font-size:12px;margin:0}.safety-note{margin-top:18px;border:1px solid #cbded3;background:#f3faf6;border-radius:9px;padding:14px;display:flex;gap:11px}.safety-note svg{width:19px;color:var(--secondary);flex:none}.safety-note strong,.safety-note small{display:block}.safety-note small{color:#52645b;margin-top:3px}@media(max-width:1050px){.setup-detail-grid{grid-template-columns:1fr}.governance-strip{grid-template-columns:1fr 1fr}.governance-item:nth-child(2){border-right:0}.governance-item:nth-child(-n+2){border-bottom:1px solid var(--line)}}@media(max-width:620px){.setup-titlebar{display:block}.setup-state{margin-top:14px;width:max-content}.governance-strip{grid-template-columns:1fr}.governance-item{border-right:0;border-bottom:1px solid var(--line)}.publish-fields{grid-template-columns:1fr}.form-actions{align-items:flex-start;flex-direction:column}.config-form{padding:15px}}
</style>
<nav class="setup-breadcrumb" aria-label="Breadcrumb"><a href="{{ route('admissions.setups.index') }}">Admin Setups</a><i data-lucide="chevron-right"></i><span>{{ $setup->category }}</span><i data-lucide="chevron-right"></i><span aria-current="page">{{ $setup->name }}</span></nav>
<header class="setup-titlebar"><div><div class="eyebrow">{{ $setup->category }} configuration</div><h1>{{ $setup->name }}</h1><p>{{ $setup->description ?: 'Controls '.$setup->consumer.'.' }}</p><code class="setup-key">{{ $setup->setup_key }}</code></div><div class="setup-state"><i data-lucide="{{ $active ? 'circle-check-big' : 'circle-alert' }}"></i>{{ $active ? 'Active · v'.$active->version : ($draft ? 'Draft pending' : 'Setup required') }}</div></header>
<section class="governance-strip" aria-label="Configuration governance summary">
<div class="governance-item"><small>Operational consumer</small><strong>{{ $setup->consumer }}</strong></div><div class="governance-item"><small>Current effective period</small><strong>{{ $active?->effective_from?->format('d M Y') ?? 'Not active' }}{{ $active?->effective_to ? ' — '.$active->effective_to->format('d M Y') : ($active ? ' — No end date' : '') }}</strong></div><div class="governance-item"><small>Missing configuration behaviour</small><strong>{{ $setup->missing_behaviour }}</strong></div><div class="governance-item"><small>Controls available</small><strong>{{ $setup->supports_preview ? 'Preview · ' : '' }}{{ $setup->supports_import ? 'Import · Export' : 'Versioned changes' }}</strong></div>
</section>
<div class="setup-detail-grid">
<section class="panel"><div class="panel-head"><div class="section-heading"><span><i data-lucide="file-pen-line"></i></span><div><h2>Prepare a new version</h2><p>Changes remain isolated until they are validated and published.</p></div></div></div><form class="config-form" method="post" action="{{ route('admissions.setups.store',$setup) }}">@csrf
<div><label class="form-label" for="configuration"><span>Configuration payload</span><small>Valid JSON required</small></label><textarea class="config-editor" id="configuration" name="configuration" spellcheck="false" required>{{ old('configuration', json_encode($latest?->configuration ?? ['enabled'=>true], JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES)) }}</textarea></div>
<div class="form-guidance"><i data-lucide="info"></i><span>This draft is created as a new immutable version. Publishing it affects new transactions from the selected effective date; completed applications and decisions keep their original version.</span></div>
<div><label class="form-label" for="change_reason"><span>Business reason for change</span><small>Required for the audit trail</small></label><input class="reason-input" id="change_reason" name="change_reason" value="{{ old('change_reason') }}" maxlength="500" placeholder="Example: Update the September 2026 application fee policy" required></div><div class="form-actions"><small>Saving creates a draft only. It does not change the live admissions process.</small><button class="btn"><i data-lucide="save"></i>Create draft version</button></div></form></section>
<aside class="panel history-panel"><div class="panel-head"><div class="section-heading"><span><i data-lucide="history"></i></span><div><h2>Version history</h2><p>Publication dates, usage and controlled status actions.</p></div></div></div><div class="history-summary"><span>{{ $setup->versions->count() }} recorded version{{ $setup->versions->count() === 1 ? '' : 's' }}</span><small>{{ $setup->versions->sum('usages_count') }} transaction references</small></div>
@forelse($setup->versions as $version)<article class="version-card"><div class="version-head"><div class="version-number"><i data-lucide="git-commit-horizontal"></i><strong>Version {{ $version->version }}</strong></div><span class="status-chip status-{{ strtolower($version->status) }}">{{ $version->status }}</span></div><div class="version-meta"><span><i data-lucide="calendar-days"></i>{{ $version->effective_from ? $version->effective_from->format('d M Y') : 'Not scheduled' }}</span><span><i data-lucide="link-2"></i>{{ $version->usages_count }} historical uses</span><span><i data-lucide="fingerprint"></i>{{ substr($version->checksum,0,10) }}…</span><span><i data-lucide="clock-3"></i>{{ $version->created_at?->format('d M Y, H:i') }}</span></div>@if($version->change_reason)<div class="change-note"><strong>Change reason:</strong> {{ $version->change_reason }}</div>@endif<details class="version-config"><summary>Inspect configuration payload</summary><pre>{{ json_encode($version->configuration, JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) }}</pre></details>
@if($version->status==='DRAFT')<form class="publish-box" method="post" action="{{ route('admissions.setups.publish',$version) }}">@csrf<h3>Schedule this version</h3><div class="publish-fields"><label>Effective from<input type="date" name="effective_from" min="{{ now()->toDateString() }}" required></label><label>Effective to <small>(optional)</small><input type="date" name="effective_to"></label></div><button class="btn"><i data-lucide="shield-check"></i>Validate and publish</button></form>@elseif($version->status==='ACTIVE')<form class="danger-action" method="post" action="{{ route('admissions.setups.status',$version) }}">@csrf @method('patch')<input type="hidden" name="status" value="INACTIVE"><small>Stops this version from being selected by new transactions.</small><button class="btn btn-secondary">Deactivate</button></form>@endif</article>@empty<div class="empty-version"><span><i data-lucide="file-plus-2"></i></span><h3>No versions created</h3><p>Complete the controlled draft form to establish this configuration.</p></div>@endforelse</aside>
</div><div class="safety-note"><i data-lucide="shield-check"></i><div><strong>Historical records are protected</strong><small>Applications, payments, reviews, decisions and letters retain the exact configuration version used at the time. A newly published version never silently recalculates completed work.</small></div></div>
@endsection
